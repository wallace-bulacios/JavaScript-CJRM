// Booleans e comporações


// Métodos que pode retornar booleans
const email = 'brucewayne@rogermelo.com.br'

    // metodo include verifica de um determinado caractere existe dentro da variavel informada
    const includes = email.includes('@') // se @ existir na strig informadqa retorna true
    console.log(includes)

    const names = ['dio', 'roger', 'robert']
    const namesIncludes = names.includes('roger')
    console.log(namesIncludes)


// Operadores de Comparação